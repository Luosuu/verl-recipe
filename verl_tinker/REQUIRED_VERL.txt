# verl_tinker — rolling against upstream main until the compatible core SHA is pinned
UPSTREAM=https://github.com/verl-project/verl.git
MODE=rolling
BRANCH=main
# This server uses recent VeRL Tinker worker internals. Replace `main` with the
# exact tested core commit when refreshing this recipe for release.
VERL_COMMIT=main
PIP_INSTALL=pip install verl@git+https://github.com/verl-project/verl.git@main
GIT_SETUP=git clone https://github.com/verl-project/verl.git && cd verl && git checkout main && git submodule update --init --recursive recipe
RECIPE_SUBMODULE_COMMIT=e44a9ca9a252322f76fe74753cc7afd812355b67
RECIPE_FOLDER=verl_tinker
RECIPE_FOLDER_LAST_COMMIT=e44a9ca9a252322f76fe74753cc7afd812355b67
NOTES=VERL_COMMIT should be tightened to an exact core verl commit once the server-side Tinker worker APIs are pinned.
REFRESH=Recompute: (cd verl && git rev-parse HEAD); (cd verl/recipe && git rev-parse HEAD); (cd verl/recipe && git log -1 --format=%H -- verl_tinker)
